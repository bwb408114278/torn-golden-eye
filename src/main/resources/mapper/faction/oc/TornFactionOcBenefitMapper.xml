<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="pn.torn.goldeneye.repository.mapper.faction.oc.TornFactionOcBenefitMapper">
    <!-- 查询OC收益排行榜 -->
    <select id="queryBenefitRanking" resultType="pn.torn.goldeneye.repository.model.faction.oc.TornFactionOcBenefitRankDO">
        SELECT b.user_id, u.nickname, SUM(benefit_money) AS benefit
        FROM torn_faction_oc_benefit b
        LEFT JOIN torn_user u ON u.deleted = 0 AND b.user_id = u.id
        WHERE b.deleted = 0 AND b.faction_id = #{factionId} AND b.oc_finish_time BETWEEN #{fromDate} AND #{toDate}
        GROUP BY b.user_id, u.nickname
        ORDER BY benefit DESC
        LIMIT 30;
    </select>
</mapper>