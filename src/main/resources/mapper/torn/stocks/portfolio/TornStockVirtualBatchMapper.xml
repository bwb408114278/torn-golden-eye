<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="pn.torn.goldeneye.repository.mapper.torn.stocks.portfolio.TornStockVirtualBatchMapper">
    <!-- 查询全部正式活跃批次 -->
    <select id="selectActiveFormalBatches" resultType="pn.torn.goldeneye.repository.model.torn.stocks.portfolio.TornStockVirtualBatchDO">
        SELECT * FROM torn_stock_virtual_batch
        WHERE ledger_type = 'FORMAL'
          AND batch_status IN ('ENTRY_PENDING', 'OPEN', 'DATA_STALE', 'EXIT_PENDING', 'DATA_STALE_EXIT')
          AND deleted = 0
    </select>

    <!-- 查询全部活跃影子批次 -->
    <select id="selectActiveShadowBatches" resultType="pn.torn.goldeneye.repository.model.torn.stocks.portfolio.TornStockVirtualBatchDO">
        SELECT * FROM torn_stock_virtual_batch
        WHERE ledger_type = 'UNLIMITED_SHADOW'
          AND batch_status IN ('ENTRY_PENDING', 'OPEN', 'DATA_STALE', 'EXIT_PENDING', 'DATA_STALE_EXIT')
          AND deleted = 0
    </select>

    <!-- 查询待买入批次(预期入场bar时间已到期) -->
    <select id="selectPendingEntryBatches" resultType="pn.torn.goldeneye.repository.model.torn.stocks.portfolio.TornStockVirtualBatchDO">
        SELECT * FROM torn_stock_virtual_batch
        WHERE batch_status = 'ENTRY_PENDING'
          AND expected_entry_bar_time <![CDATA[<=]]> #{currentTime}
          AND deleted = 0
    </select>

    <!-- 查询待卖出批次(预期平仓bar时间已到期) -->
    <select id="selectPendingExitBatches" resultType="pn.torn.goldeneye.repository.model.torn.stocks.portfolio.TornStockVirtualBatchDO">
        SELECT * FROM torn_stock_virtual_batch
        WHERE batch_status = 'EXIT_PENDING'
          AND expected_exit_bar_time <![CDATA[<=]]> #{currentTime}
          AND deleted = 0
    </select>
</mapper>
